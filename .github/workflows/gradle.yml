name: CI
on:
  push:
    branches:
      - '*'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: 17
          distribution: 'adopt'
      - name: set permisssion for Gradle
        run: chmod +x gradlew    
      - name: Build with Gradle
        run: ./gradlew build

      - name: List Target Directory
        run: |
          cd build
          ls -la
        
      - name: List Target Directory
        run: |
          cd build/libs
          ls -la

      - name: Veracode Upload And Scan
        uses: veracode/veracode-uploadandscan-action@0.2.1
        with:
          appname: 'VeraDemo'
          createprofile: false
          filepath: 'build/libs/*.jar'
          vid: 'b6c6f27f0e1fe68ab3f8fca2'
          vkey: 'ca774c6ae284f0406aa8570d'